{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Maven Clean Install",
            "type": "shell",
            "command": "mvn clean install",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/socialmedia"
            },
            "problemMatcher": []
        }
    ]
}